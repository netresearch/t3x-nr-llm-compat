# Integrations are opt-in: installing nr_llm_compat changes NOTHING until an
# integration is explicitly enabled here. Once enabled, the integration is
# fail closed — requests error out when nr-llm is unavailable instead of
# falling back to the third-party extension's own provider.

# cat=integrations; type=boolean; label=AI SEO Helper (passionweb/ai-seo-helper): Route its OpenAI calls through nr-llm. Only takes effect when a supported version is installed and the contract check passes — see "vendor/bin/typo3 nrllm:compat:status".
integrations.ai_seo_helper = 0

# cat=integrations; type=boolean; label=T3AI (nitsan/ns-t3ai): Route its OpenAI calls (SEO suggestions and RTE content generation) through nr-llm. Only takes effect when a supported version is installed and the contract check passes — see "vendor/bin/typo3 nrllm:compat:status".
integrations.ns_t3ai = 0

# cat=integrations; type=boolean; label=AI File Metadata (mfd/ai-filemetadata): Route its alt-text vision calls through nr-llm; its own token statistics keep working. Only takes effect when a supported version is installed and the contract check passes — see "vendor/bin/typo3 nrllm:compat:status".
integrations.ai_filemetadata = 0

# cat=integrations; type=boolean; label=Texter (in2code/texter): Point its official llmRepositoryClass hook at nr-llm; prompt prefix and per-page conversation history keep working. Only takes effect when a supported version is installed and the contract check passes — see "vendor/bin/typo3 nrllm:compat:status".
integrations.texter = 0

# cat=integrations; type=boolean; label=Exception Solver (eliashaeussler/typo3-solver): Point its official provider setting at nr-llm. Only takes effect when a supported version is installed and the contract check passes — see "vendor/bin/typo3 nrllm:compat:status".
integrations.solver = 0
